class Activity {
  String activityType;
  double distance;
  double duration;
  double calories;
  String docId;
  DateTime timestamp;

  Activity({this.activityType, this.distance, this.duration, this.calories, this.docId, this.timestamp});
}